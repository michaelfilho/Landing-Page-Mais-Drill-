# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup.

# Mais Drill — não há autenticação no app (landing pública).
# WhatsApp de checkout/atendimento: PLACEHOLDER 5511999999999 (definido em /app/frontend/src/data/products.js) — substituir pelo número real da marca.
# Newsletter estática demonstrativa: não envia nem armazena inscritos.
